pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode = "strict"
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "SmsForwarder"
include ":app"